Throwaway Ed25519 demo keys, generated only to make `asr-whisper-tiny-npu.manifest.json`,
`asr-whisper-tiny-npu.weights-update.round17.manifest.json` and
`asr-moonshine-iree-vulkan-de.weights-update.decoder.manifest.json` real, independently
verifiable examples (run `verify_manifest.py`; the signing side is `sign_manifest.py`).
Regenerated 2026-09-14 for the public release (keyids `publisher-2026-09`,
`aggregator-2026-09`). **Not production keys** — the private halves were never committed
anywhere and were discarded after signing. Real trust roots are provisioned per OQ6
(Key management / trust roots), not shipped inside this repo.
